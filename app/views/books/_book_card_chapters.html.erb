<%= turbo_frame_tag book_card_chapters_turbo_frame_id(book) do %>
  <%= render(BadgeCountsComponent.new(
    custom_classes: "mr-1.5 #{'!text-emerald-700 !bg-emerald-100' if book.completed?}",
    numerator: book.completed_chapters_count,
    denominator: book.chapters_count,
  ))%>
<% end %>