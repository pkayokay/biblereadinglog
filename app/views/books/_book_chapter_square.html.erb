<%= turbo_frame_tag book_checklist_square_turbo_frame_id(book, chapter_data["chapter_number"]) do %>
  <%= button_to chapter_data["chapter_number"], reading_log_book_toggle_chapter_path(book, book.reading_log, chapter_number: chapter_data["chapter_number"], is_completed: chapter_data["completed_at"].present?), method: :patch, id: "", class: "h-[50px] md:h-[40px] w-[50px] md:w-[40px] cursor-pointer", form: {class: "#{ 'shadow-green-100 border-green-300 bg-green-50 hover:bg-green-100' if chapter_data["completed_at"]} hover:bg-gray-50 border shadow-sm rounded-lg inline-flex items-center justify-center text-base" }%>
<% end %>
