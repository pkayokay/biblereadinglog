<%= turbo_frame_tag book_checklist_square_turbo_frame_id(book, chapter_data["chapter_number"]) do %>
  <%= button_to chapter_data["chapter_number"], toggle_chapter_reading_log_book_path(book.reading_log, book, chapter_number: chapter_data["chapter_number"], is_completed: chapter_data["completed_at"].present?), method: :patch, id: "", class: "h-[44px] w-[44px] cursor-pointer", form: {class: "#{ theme_values[:square] if chapter_data["completed_at"]} hover:bg-gray-50 border shadow-sm rounded-lg inline-flex items-center justify-center text-base" }%>
<% end %>
