<div class="max-w-xs mx-auto">
  <h1 class="text-center font-medium text-2xl"><%= @book.name %></h1>
  <%= turbo_frame_tag "book-checklist-#{@book.id}" do %>
    <div data-chapter-container-id="book-checklist-<%= @book.id %>" class="mt-5 grid md:grid-cols-6 grid-cols-4 md:gap-3 gap-5 text-center">
      <% @book.chapters_data.each do |chapter_data| %>
        <%= render "book_chapter_square", book: @book, chapter_data: chapter_data %>
      <% end %>
    </div>
  <% end %>
</div>