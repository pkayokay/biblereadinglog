<!DOCTYPE html>
<html>
  <head>
    <title>Bible Reading Log</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-neutral-50 pb-14">
    <% if user_signed_in? %>
      <%= render "shared/flash" %>
      <nav class="bg-white border-b border-stone-200">
        <div class="w-full px-5 py-4 flex mx-auto justify-between items-center">
          <%= link_to root_path, class: "flex space-x-2 items-center text-lg font-medium text-white tracking-[0.5px]" do %>
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="#333" class="w-8 h-8">
              <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 3.75V16.5L12 14.25 7.5 16.5V3.75m9 0H18A2.25 2.25 0 0 1 20.25 6v12A2.25 2.25 0 0 1 18 20.25H6A2.25 2.25 0 0 1 3.75 18V6A2.25 2.25 0 0 1 6 3.75h1.5m9 0h-9" />
            </svg>
            <span class="hidden md:block text-neutral-800">Bible Reading Log</span>
          <% end %>
          <div class="flex items-center space-x-2">
            <%= link_to root_path, class: "flex space-x-1 py-1.5 px-5 text-stone-800 rounded-md" do %>
              <span>Home</span>
            <% end %>
            <div
              data-controller="dropdown"
              data-action="click@window->dropdown#hide touchstart@window->dropdown#hide keydown.up->dropdown#previousItem keydown.down->dropdown#nextItem keydown.esc->dropdown#hide"
              class="inline-block relative">
              <button data-action="dropdown#toggle:stop" class="h-10 w-10  bg-neutral-400 rounded-full text-white">
                <span class="uppercase font-medium"><%= current_user.initials %></span>
              </button>
              <div data-dropdown-target="menu" class="hidden absolute top-6 right-0 z-10 mt-5 flex w-screen max-w-max">
                <div class="bg-white shadow-sm rounded-md border overflow-hidden w-40">
                  <%= link_to  account_path, class: "flex space-x-1 py-1.5 px-5  hover:bg-neutral-100", data: { dropdown_target: "menuItem"} do %>
                    <span class="opacity-70">Account</span>
                  <% end %>
                  <% if current_user.is_admin? %>
                    <%= link_to admin_path, class: "flex space-x-1 py-1.5 px-5  hover:bg-neutral-100", data: { dropdown_target: "menuItem"} do %>
                      <span class="opacity-70">Admin</span>
                    <% end %>
                  <% end %>
                  <hr class="border-t" />
                  <%= button_to sign_out_path, method: :delete, class: "text-left no-underline block pl-4 w-full py-1.5 text-5ray-900 bg-white hover:bg-gray-100 whitespace-nowrap focus:bg-gray-100", data: { dropdown_target: "menuItem" } do %>
                   <span class="opacity-70">Log out</span>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
      <main class="p-5 pt-10 mx-auto w-full max-w-4xl">
        <%= render "shared/breadcrumbs" %>
        <%= yield %>
      </main>
    <% else  %>
      <%= render "shared/flash", custom_top_class: "!top-10" %>
      <main class="p-5 mt-20">
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
