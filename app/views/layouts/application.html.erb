<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= content_for?(:html_title) ? yield(:html_title) : "Bible Reading Log" %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/manifest.json" />
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">

    <% if @skip_turbo_cache_control == true %>
      <meta name="turbo-cache-control" content="no-cache">
    <% end %>
  </head>

  <body class="<%= "bg-stone-50 flex-col pb-16" if user_signed_in? %>">
    <% if user_signed_in? %>
      <div id="flash">
        <%= render "shared/flash" %>
      </div>
      <nav class="w-full">
        <div class="w-full px-4 py-2.5 flex mx-auto justify-between items-center">
          <div class="flex items-center space-x-4">
            <%= link_to root_path, class: "flex space-x-2 items-center text-lg text-nowrap font-medium text-neutral" do %>
              <%= render "shared/logo" %>
              <span class="text-neutral-800">Bible Reading Log</span>
            <% end %>
          </div>
          <div class="flex items-center space-x-2">
            <% if current_user.confirmed? %>
              <%= link_to root_path, class: "hidden sm:inline space-x-1 py-1.5 px-2 opacity-70 hover:opacity-100 rounded-md" do %>
                <span>Reading Logs</span>
              <% end %>
            <% end %>

          </div>

        </div>
      </nav>
      <main data-turbo-prefetch="false" class="px-8 <%= request.path == root_path ? 'pt-8' : 'py-8' %> flex-1 relative md:bg-white md:border border-gray-200 md:shadow-sm md:rounded-lg mx-auto w-full max-w-2xl">
        <%= yield %>
      </main>
    <% else  %>
      <%= render "shared/flash", custom_top_class: "!top-10" %>
      <main class="p-5 mt-16 mb-10">
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
