<!DOCTYPE html>
<html>
  <head>
    <title>Biblereadinglog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-zinc-50">
    <% flash_message = flash[:notice] || flash[:alert] %>
    <% if flash_message.present? %>
      <div class="mb-3 border rounded bg-gray-200 px-2 py-1 inline-block"><%= flash_message %></div>
    <% end %>

    <% if user_signed_in? %>
      <nav class="px-5 py-3 flex mx-auto justify-between items-center">
        <%= link_to "📚", root_path, class: "text-4xl font-semibold" %>
        <div
          data-controller="dropdown"
          data-action="click@window->dropdown#hide touchstart@window->dropdown#hide keydown.up->dropdown#previousItem keydown.down->dropdown#nextItem keydown.esc->dropdown#hide"
          class="inline-block relative">
          <button data-action="dropdown#toggle:stop" class="pl-4 pr-2.5 py-1 bg-white border text-sm rounded-md shadow-sm">
            <%= current_user.email %>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" class="inline-block fill-current h-4 w-4"><path d="M9.293 12.95l.707.707L15.657 8l-1.414-1.414L10 10.828 5.757 6.586 4.343 8z"></path></svg>
          </button>
          <div data-dropdown-target="menu" class="hidden absolute top-4 right-0 z-10 mt-5 flex w-screen max-w-max">
            <div class="text-sm bg-white shadow-md rounded-md border overflow-hidden w-32">
              <%= link_to root_path, class: "flex space-x-1 py-2 px-4 hover:bg-neutral-100", data: { dropdown_target: "menuItem"} do %>
                <span>Home</span>
              <% end %>
              <%= link_to  account_path, class: "flex space-x-1 py-2 px-4  hover:bg-neutral-100", data: { dropdown_target: "menuItem"} do %>
                <span>Account</span>
              <% end %>
              <hr class="border-t" />
              <a data-dropdown-target="menuItem" href="#" class='no-underline block pl-4 py-2 text-gray-900 bg-white hover:bg-gray-100 whitespace-nowrap focus:bg-gray-100'>Sign Out</a>
            </div>
          </div>
        </div>
      </nav>
    <% end %>
    <main class="p-5 pt-2 mx-auto w-full max-w-3xl">
      <%= render "shared/breadcrumbs" %>
      <%= yield %>
    </main>
  </body>
</html>
